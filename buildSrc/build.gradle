allprojects {
    apply plugin: 'groovy'

    dependencies {
        compile gradleApi()
        compile localGroovy()
    }
}

dependencies {
    runtime project(':gradle-foo-plugin')
    runtime project(':gradle-bar-plugin')
    runtime project(':gradle-baz-plugin')
}
